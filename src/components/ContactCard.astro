---
const { label, value, href } = Astro.props;
---

<a href={href} class="block rounded-2xl border border-slate-800 bg-slate-900 p-5 hover:border-cyan-500">
  <p class="text-sm text-slate-400">{label}</p>
  <p class="mt-1 font-semibold text-white">{value}</p>
</a>