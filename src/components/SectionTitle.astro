---
const { title, subtitle } = Astro.props;
---

<div class="mb-10 text-center">
  <h2 class="text-3xl font-bold text-white md:text-4xl">{title}</h2>
  {subtitle && <p class="mt-3 text-slate-400">{subtitle}</p>}
</div>